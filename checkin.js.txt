async function hadir(){

const id =
new URLSearchParams(
window.location.search
).get("id")

await supabase
.from("reservations")
.update({attendance:true})
.eq("id",id)

alert("Hadir")

}
